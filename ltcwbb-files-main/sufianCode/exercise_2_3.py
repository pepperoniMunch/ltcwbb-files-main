# 2.3  announce_pitch

def announce_pitch(player_name, pitch_type):
    """
    a function that announces the players name with the pitch type,
    akin to how a color commentator would during broadcast
    """
    print(f"{player_name} with the {pitch_type}!")
